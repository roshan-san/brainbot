{
  "name": "@brainbot",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "type-check": "pnpm -r run type-check"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "nx": "^21.6.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}